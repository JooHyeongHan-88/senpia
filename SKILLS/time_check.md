---
name: time_check
description: 현재 시각을 확인하고 시각 로그를 마크다운으로 사용자에게 시연한다
trigger:
  - 지금 시간
  - 현재 시각
  - 몇 시야
  - 몇 시인가요
  - what time
priority: 5
requires_tools:
  - now
---

# 시간 확인 작업 가이드

사용자가 현재 시각을 묻거나 시간 기록이 필요한 작업을 요청할 때 따르는 표준 절차다.
`now` 도구로 시각을 가져온 뒤, 저장·렌더링은 의존 SKILL `_time_log_render` 에 위임해
마크다운 산출물로 사용자에게 시연한다.

## 단계

1. **시각 조회** — `now()` 도구로 현재 시각을 ISO 8601 문자열로 가져온다.
2. **시각 로그 저장 및 렌더링** — `activate_skill('_time_log_render')` 로 시각 로그 저장·렌더링
   절차를 켜고, 켜진 지침을 그대로 수행한다. (서술이 아니라 도구 이름·인자를 리터럴로 명시해야
   비공개 SKILL이 결정론적으로 켜진다.)
3. **자연어 응답** — 사용자에게 "현재 시각은 {time} 입니다" 형태로 한국어 응답한다.
   필요하면 우측 패널에 추가 시각 자료가 있다는 안내 한 줄을 덧붙인다.

## 주의

- 각 도구 호출은 한 번씩만 — 동일 인자로 연속 호출하면 loop-guard 가 차단한다.
- 저장·렌더링 단계의 세부 규칙(경로 형식 등)은 `_time_log_render` SKILL 본문에 있다.
